<Window x:Class="Tienda_Abarrotes.View.ManejoUsuarioViewModel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Tienda_Abarrotes.ViewModel"
        mc:Ignorable="d"
        Title="Gestor de Usuarios" Height="600" Width="1200"
        Background="#F4F6F9"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#2D3E50" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <DockPanel>
                <TextBlock Text="Gestión de Usuarios" Foreground="White" FontSize="22" FontWeight="Bold" DockPanel.Dock="Left"/>
                <Button Content="Refrescar" Command="{Binding RefreshCommand}" Background="#03A9F4" Foreground="White" Width="120" DockPanel.Dock="Right" Margin="10,0,0,0"/>
                <Button Content="Regresar" Click="Button_Click"  Background="DarkRed" Foreground="White" Width="120" DockPanel.Dock="Right" Margin="10,0,0,0"/>
                <Button Content="Agregar" Command="{Binding AddCommand}" Background="#4CAF50" Foreground="White" Width="120" DockPanel.Dock="Right" Margin="10,0,0,0"/>
            </DockPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Correo" Binding="{Binding Email}" Width="2*"/>
                <DataGridTemplateColumn Header="Acciones" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Eliminar" 
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" 
                                    Background="#F44336" Foreground="White"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <StackPanel Margin="10,0">
                <TextBlock Text="Usuario" FontWeight="Bold"/>
                <TextBox Width="150" Text="{Binding SelectedUser.UserName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Margin="10,0">
                <TextBlock Text="Correo" FontWeight="Bold"/>
                <TextBox Width="150" Text="{Binding SelectedUser.Email, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <Button Content="Guardar" Command="{Binding EditCommand}" Background="#2196F3" Foreground="White" Width="120" VerticalAlignment="Bottom" Height="30" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>