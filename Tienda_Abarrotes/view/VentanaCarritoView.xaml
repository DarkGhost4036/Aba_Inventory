<Window x:Class="Tienda_Abarrotes.View.VentanaCarritoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tienda_Abarrotes.View"
        mc:Ignorable="d"
        Title="VentanaCarritoView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Resumen de Venta" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ItemsCarrito}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Cant" Binding="{Binding Cantidad}" Width="60"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio, StringFormat={}{0:C}}"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat={}{0:C}}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock Text="{Binding TotalAPagar, StringFormat='Total a cobrar: {0:C}'}" 
                   FontSize="28" Foreground="#27ae60" FontWeight="Bold"/>

            <Button Content="Confirmar Venta y Cobrar" 
                Command="{Binding FinalizarVentaCommand}"
                Background="#27ae60" Foreground="White" Padding="15,10" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
